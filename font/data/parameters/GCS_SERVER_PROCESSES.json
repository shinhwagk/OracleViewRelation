{ "property" : { "Oracle RAC" : "Multiple instances can have different values.", "Modifiable in a PDB" : "No", "Modifiable" : "No", "Parameter type" : "Integer", "Range of values" : "<code>1</code> to <code>36</code>", "Default value" : "If 1 - 3 CPUS, then <code>1</code></p>\n<p>If 4 - 15 CPUs, then <code>2</code></p>\n<p>If 16 or more CPUs, then <code>2</code> + (CPUs / 32). If the result includes a fraction, then the fraction is disregarded. For example, if you had 20 CPUs, then 2 + (20 / 32) would equal 2 GCS processes.<a id=\"fn_1\" href=\"#fn_1\" onclick='footdisplay(1,\"On certain operating systems, the RDBMS optimizes the default allocation of background GCS server processes based on the core to thread ratio of the CPU. \")'><sup>Foot&nbsp;1</sup></a></p>\n<p>If <code>CLUSTER_DATABASE</code> is set to <code>false</code>, then <code>0</code></p>\n<p>If Oracle ASM, then <code>1</code>", "Basic" : "No" }, "section" : "<p class=\"tablefootnote\"><sup class=\"tablefootnote\">Footnote&nbsp;1</sup></p>\n<p>On certain operating systems, the RDBMS optimizes the default allocation of background GCS server processes based on the core to thread ratio of the CPU.</p>\n<div class=\"section\">\n<p><code>GCS_SERVER_PROCESSES</code> specifies the number of background GCS server processes (LMS0, ... LMS9 and LMSa, ... LMSz) to serve the inter-instance traffic among Oracle RAC instances. GCS server processes are only seen in an Oracle RAC environment.</p>\n</div>\n<!-- class=\"section\" --></div>\n</div>\n<!-- class=\"ind\" --><!-- Start Footer -->\n</div>\n<!-- add extra wrapper close div-->" }