{ "notes" : [ "<p>This parameter is meaningful only on standby databases. Its settings are ignored on a primary database.</p>", "<p>This initialization parameter is available starting with Oracle Database 12<span class=\"italic\">c</span> Release 2 (12.2.0.1).</p>" ], "property" : { "Oracle RAC" : "All instances should use the same value.", "Modifiable in a PDB" : "No", "Modifiable" : "<code>ALTER SYSTEM</code>", "Parameter type" : "String", "Default value" : "<code>*</code> if no value is specified for this parameter in the <code>init.ora</code> file", "Syntax" : "<code>ENABLED_PDBS_ON_STANDBY = <span class=\"codeinlineitalic\">PDB-list</span></code>", "Basic" : "No" }, "section" : "<p><code>ENABLED_PDBS_ON_STANDBY</code> specifies which pluggable databases (PDBs) to replicate on an Oracle Data Guard standby database.</p>\n<div class=\"infobox-note\" id=\"GUID-A2EC2381-D9C0-4612-87C9-A225C7BCE8C4__GUID-6DCED2F8-6EEC-49EB-BD55-A670788975B2\">\n<p class=\"notep1\">Note:</p>\n<p>This parameter is meaningful only on standby databases. Its settings are ignored on a primary database.</p>\n</div>\n<p><span class=\"italic\">PDB-list</span> accepts a list of PDB names represented by glob patterns such as &ldquo;PDB?&rdquo; or &ldquo;PDB*a&rdquo;, &ldquo;PDB2&rdquo;. The glob pattern rules are like those of UNIX shells. The asterisk (*) and question mark (?) wildcard characters are supported. The ? wildcard character matches exactly one unknown character, and the * wildcard character matches any number of unknown characters. Also, the minus sign (-) character can be used as the first character in a PDB name to indicate that the PDB should be excluded on the standby database.</p>\n<p>The following characters are valid in a PDB name: alphanumeric characters, underscore (_), number sign (#), and dollar sign ($). No other characters are valid. Oracle removes double quotation before processing the PDB name. Therefore you cannot use double quotation marks to embed other characters in the name. The PDB name is case insensitive. These are the same naming conventions as for a database name (as described in the <code>DB_NAME</code> initialization parameter description).</p>\n<p>These rules apply for the <code>ENABLED_PDBS_ON_STANDBY</code> parameter:</p>\n<ul style=\"list-style-type: disc;\">\n<li>\n<p>Any PDB names that match glob patterns starting with minus sign (-) will not be part of the standby database.</p>\n</li>\n<li>\n<p>If a PDB name matches several patterns in the <span class=\"italic\">PDB-list</span> at the same time, the rightmost matched pattern has the highest precedence.</p>\n</li>\n<li>\n<p>If this parameter is not specified in the <code>init.ora</code> file, it is assumed that &ldquo;*&rdquo; is specified by default.</p>\n</li>\n<li>\n<p>If one or more patterns are specified in this parameter, it is assumed that &ldquo;-*&rdquo; is implicitly specified in the leftmost position. For example, <code>ENABLED_PDBS_ON_STANDBY=&ldquo;PDB1&rdquo;, &ldquo;PDB2&rdquo;</code> is equivalent to <code>ENABLED_PDBS_ON_STANDBY=&ldquo;-*&rdquo;, &ldquo;PDB1&rdquo;, &ldquo;PDB2&rdquo;</code>. It means that a PDB name that does not match any patterns is not allowed to be part of the standby.</p>\n</li>\n</ul>\n<div class=\"infobox-note\" id=\"GUID-A2EC2381-D9C0-4612-87C9-A225C7BCE8C4__GUID-EC812666-5399-4017-B079-A371F05AD58A\">\n<p class=\"notep1\">Note:</p>\n<p>This initialization parameter is available starting with Oracle Database 12<span class=\"italic\">c</span> Release 2 (12.2.0.1).</p>\n</div>\n</div>\n<!-- class=\"section\" -->\n<div class=\"section\">\n<p class=\"subhead1\">Examples</p>\n<p>These examples assumes that nine new PDBs named PDB1A, PDB1B, PDB1C, PDB2A, PDB2B, PDB2C, PDB3A, PDB3B, and PDB3C are being added to the primary database.</p>\n<ol>\n<li>\n<p>If STANDBY1 sets <code>ENABLED_PDBS_ON_STANDBY=&ldquo;*&rdquo;</code> on STANDBY1, then all nine PDBs will be created on STANDBY1.</p>\n</li>\n<li>\n<p>If STANDBY1 sets <code>ENABLED_PDBS_ON_STANDBY=&ldquo;PDB1*&rdquo;</code> on STANDBY1, then PDB1A, PDB1B, and PDB1C will be created on STANDBY1.</p>\n</li>\n<li>\n<p>If STANDBY1 sets <code>ENABLED_PDBS_ON_STANDBY=&ldquo;PDB*A&rdquo;</code> on STANDBY1, then PDB1A, PDB2A, and PDB3A will be created on STANDBY1.</p>\n</li>\n<li>\n<p>If STANDBY1 sets <code>ENABLED_PDBS_ON_STANDBY=&ldquo;PDB1*&rdquo;, &ldquo;-PDB*A&rdquo;</code> on STANDBY1, then PDB1B and PDB1C will be created on STANDBY1.</p>\n</li>\n<li>\n<p>If STANDBY1 sets <code>ENABLED_PDBS_ON_STANDBY=&ldquo;*&rdquo;, &ldquo;-PDB*A&rdquo;, &ldquo;-PDB*B&rdquo;</code> on STANDBY1, then PDB1C, PDB2C and PDB3C will be created on STANDBY1. All other PDBs match &ldquo;*&rdquo;, and the rightmost pattern has the higher precedence, so they are excluded on the standby.</p>\n</li>\n<li>\n<p>If STANDBY1 sets <code>ENABLED_PDBS_ON_STANDBY=&ldquo;*&rdquo;, &ldquo;-PDB*A&rdquo;, &ldquo;PDB2A&rdquo;</code> on STANDBY1, then PDB1A and PDB3A are excluded, but all other PDBs including PDB2A will be created.</p>\n</li>\n</ol>\n</div>\n<!-- class=\"section\" --></div>\n</div>\n<!-- class=\"ind\" --><!-- Start Footer -->\n</div>\n<!-- add extra wrapper close div-->" }